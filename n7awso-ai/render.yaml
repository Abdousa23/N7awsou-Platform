services:
  - type: web
    name: n7awso-ai
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      npx prisma generate
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000